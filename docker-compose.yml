version: '3.8'
services: 
  db:
    image: postgres
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: toor
      POSTGRES_DB: student
      POSTGRES_PORT: 5432
      POSTGRES_HOST: 127.0.0.1